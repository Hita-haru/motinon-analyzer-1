# Motion-analyzer-1（Motiana1） 開発計画
## 目的
カメラからの画像の入力をリアルタイムで処理し、最終的にはモーションキャプチャーの機能を持つものを作成することを目的とする。  
現在の進捗を鑑み、以下の内容を一旦の目標として開発を進める。  
カメラからの画像入力をopencvを使用して処理し、人間を認識し画像にマークしてそれを画面に出力する。  
## 共同開発者へ
./<名前>-workspaceを作成するのでアカウントを作成したら@Hita-haruへ連絡を  
gitの使い方を後ろに書きました <-NEW!  
初期設定のやり方とbranchの切り方のルールとか書いてあるので**絶対見てね**！  
⚠️conflictを防ぐため各種ファイルは必ずそれぞれのworkspaceに保存すること。  
↑conflictするとあとからめちゃくちゃ面倒なことになるから気をつけて  
プライバシー保護のため実名を使用せずユーザー名を使用すること。  
自分のやるべき範囲外を操作するとconflictする原因になるため指定された範囲を基本的に操作すること。  
OpenCVを使用する際は`#include <opencv2/opencv.hpp>`でincludeしてください。  
## 規格
- 使用する言語  
    基本的にC++を使用  
- 使用するライブラリー・ヘッダーファイル
    iostream, OpenCV  
    随時追加する予定  
- 使用するコンパイラ・その他ツールなど  
    linux向けに作成。最終ビルドは@Hita-haruが担当。  
    コンパイラ: clang++  
    make（一部ライブラリ用にcmakeを使用する）  
## 作成するコード
- main.cpp  
    main関数を記述。  
    @Hita-haru  
- vdload.cpp  
    動画をopencvで操作可能な形で読み込む  
    （解像度を落とし、フレームを間引いて（3~5fps程度）処理の高速化）  
    @zanzouda  
- hmdetect.cpp  
    画像の中で人間を探す  
    （未分担）  
- markrender.cpp  
    人間の位置にマークし、画面に表示する  
    （未分担）  
- tuihandler.cpp: 他のプログラム待ち  
    TUIでの操作コンソールを表示する  
    @Hita-haru  
- 各種ヘッダーファイル.h  
    それぞれのリンクのためのファイル  
    @Hita-haru  

## Gitの使い方
### 初期設定のやり方
大前提として、パソコンでやってください。それもmacとかじゃない普通のwindowsパソコンで。もしない場合は要相談！  
まずはgit bashを入れます。これは`git bash 入れ方`とかで調べればめちゃくちゃ出てきます。 便利なサイト -> https://google.com  
【注意】インストールするときWindows Explorer integration→Git bash hereはオフにしないでね  
インストール後は一回再起動しといてね  
あとgithubアカウントも作ってください。https://github.com  
入れたらこのプログラムを入れとくフォルダを作りましょう。作らないとバラけるし最悪他の人にも迷惑がかかります。おすすめはドキュメントフォルダにmotianaって名前のフォルダを作ってください。  
作ったフォルダを開いてフォルダの何もないとこで右クリックします  
多分`Git bash here`みたいなやつが出ると思います。それを押します。出なかったら`その他のオプションを表示`みたいなのを押してから探してみてください。  
↑インストール後再起動しないと出ない  
黒い画面に白い文字のやつが出ると思います。 <- **かっこいい！**  
そこにコマンドを打っていきます。あらかじめgithubアカウントのメールアドレスとユーザー名をメモかなんかでわかるようにしといてください。  
```Bash
git config --global user.name "<ユーザー名>"
git config --global user.email "<メールアドレス>"
```  
もちろん、バカ正直にコピペするんじゃなくて<ユーザー名>と<メールアドレス>とかは書き換えてくださいね  
↑これからも<>でくくったとこは書き換えてね  
これでプログラマーの仲間入りです！やったね！  
次は暗号鍵の追加です！説明めんどいのでgeminiちゃんが説明してくれます  

---

1. 既存の鍵がないか確認する
すでに鍵を持っている可能性があるので、念のため確認します。以下のコマンドを入力して「Enter」を押してください。

```bash
ls -al ~/.ssh
```

* `id_rsa.pub` や `id_ed25519.pub` というファイルが表示されたら、すでに鍵は作成されています（手順4へ進んでOK）。
* 「No such file or directory」と出たり、何も表示されなければ、新しく作成します。

---

1. SSH鍵を作成する
現在、最も推奨されている**Ed25519**という形式で鍵を作ります。以下のコマンドの `your_email@example.com` の部分を、自分のメールアドレスに書き換えて入力してください。

```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```

実行すると、いくつか質問されますが、**基本的にはすべて何も入力せず「Enter」を連打してOK**です。

* **保存場所の確認:** そのまま「Enter」。
* **パスフレーズ（暗号）:** 設定したい場合は入力しますが、空のまま「Enter」を2回押せば、パスワードなしでログインできるようになります。



---

1. SSHエージェントに鍵を登録する
作成した鍵をシステムに認識させます。

1.  **エージェントを起動する**
    ```bash
    eval "$(ssh-agent -s)"
    ```
2.  **鍵を登録する**
    ```bash
    ssh-add ~/.ssh/id_ed25519
    ```
    （※もし手順3で別の名前を付けた場合は、そのファイル名にしてください）

---

1. 公開鍵をコピーしてGitHub等に貼り付ける
最後に、作成した「鍵のペア」のうち、**公開鍵（.pub）**の中身をコピーして、GitHubなどのサービスに登録します。

1.  **中身を表示する**
    ```bash
    cat ~/.ssh/id_ed25519.pub
    ```
2.  表示された `ssh-ed25519 AAA...` から始まる長い文字列を、マウスでドラッグしてコピーします。
3.  GitHubの場合：
    * **Settings** ＞ **SSH and GPG keys** ＞ **New SSH key** を開く。 <- 注: アカウントの設定です。右上のアカウントアイコンをクリックしてSettingsを開く
    * Titleに適当な名前（例：My Windows PC）を付け、Keyの欄にコピーした内容を貼り付けて保存！

---

1. 接続テスト
設定が正しくできたか、以下のコマンドで確認してみましょう。

```bash
ssh -T git@github.com
```

> **Hi [ユーザー名]! You've successfully authenticated...**

というメッセージが返ってくれば、設定完了です！お疲れ様でした。

---

- 補足：秘密鍵と公開鍵の違い
暗号鍵は「ペア」で動きます。初心者の方が一番やってはいけないミスは、**秘密鍵を他人に渡すこと**です。

| 鍵の種類 | ファイル名 | 役割 | 扱い |
| :--- | :--- | :--- | :--- |
| **公開鍵** | `id_ed25519.pub` | サーバー（GitHub等）に渡すもの | **公開してOK** |
| **秘密鍵** | `id_ed25519` | 自分のPCに保存しておくもの | **絶対に見せない** |

---

できたら次は、プロジェクトをgithubにひもづけていきましょう  
今度は多分コピペでいいかも。  
```Bash
git init
git branch -m main
git remote add git@github.com:Hita-haru/motinon-analyzer-1.git
git pull origin main
```  
こーするとなんかファイルがダウンロードされます。  
これで完成！おめでとう！  
### 【重要】branchについて  
コードの煩雑化・バグの重症化を防ぐため、以下の新しい方法で操作してください。以降のgit操作では、mainへのpushを禁止します。  
```Bash
git checkout -b <branch名>
```  
↑これ、難しく考えると分かりづらいんですが`checkout -b`ってのは`<branch名>`のブランチを作ってそれに移動してるだけなんですよね  
だから逆に以降既存のbranchにpushしたいなーって思ったら`-b`はいらないです。  
`git checkout <branch名>`してあげちゃえばそれでOK  
※<branch名>には以下の規定に沿ったものを使用し、新規に作成してください。  
`<操作内容>/＜ユーザー設定＞`  
＜操作内容＞には操作の内容に応じて以下の選択肢の中から選択できます。  
- fix
    バグ修正、typoの修正など  
- feature  
    機能の追加。新たにコードを書き足すときなどに使用  
- docs  
    ドキュメントの修正。マニュアルや各種.mdファイルなどの編集用  
- refactor  
    コードやファイルの整理など用。機能に変更を加えない。  
<ユーザー設定>には作業の内容に応じた名前を自由に指定できます。他のbranchと重複しないようにしてください。  
pushの際は以下のようにすればokです  
```Bash
git add .
git commit -m "＜コメント＞"
git push origin <branch名>
```  
直後にGithubを開きます。 https://github.com/Hita-haru/motinon-analyzer-1  
すると上部に "Compare & pull request" と書かれている黄色いバーが出てくると思うので、  
緑色のボタンをクリックすると自動的にプルリクエスト(以下PR)が作成されます。  
ただそんなボタン出てこないことも結構あります。  
そんなときは上のとこにある`Pull requests`をクリックしましょう。  
そうしたら緑色の`New pull request`ボタンを押します。  
色々出てくると思うので、下にスクロールしてさっきpushした`<branch名>`をクリックします。（なかったら多分pushできてない）  
そして緑色の`Create pull request`をクリックします。  
まあ色々出てくるんですが、そのまままた緑色の`Create pull request`をクリックします。  
そしたら**完成**です
### まとめ
コードを編集後、以下のようにコマンドを打ち込めば大抵の場合OKです  
あとPRの作り忘れに注意！PM（@Hita-haru）が確認できないので何もやってないことになっちゃいます  
（やらないとは思うけどバカ正直にコピペしてそのままEnterとかはしないでね！）  
```Bash
git checkout -b <branch名>
git add .
git commit -m "<コメント>"
git push origin <branch名>
```